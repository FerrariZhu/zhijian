# 知见 Design System

> 版本：1.1  
> 适用范围：知见微信小程序与 H5  
> 设计定位：权威、克制、内容优先。以品牌红建立识别，以高质量排版承载长文阅读。  
> 产品契约：[PRD v1.2](./PRD.md)。视觉与产品冲突时，以 PRD 的范围为准，以本文的令牌和组件规格落地。

## 1. 设计原则

### 1.1 内容优先

- 界面以白色和灰阶为主，将注意力让给标题、正文及新闻图片。
- 避免大面积装饰、渐变、阴影和高饱和背景。
- 使用排版、留白和细分割线建立信息层级。
- 未做的功能不出现入口，也不用「建设中」占位。

### 1.2 红色表达状态

- 品牌红仅用于品牌区域、选中状态、关键操作和重点栏目。
- 普通正文、次要按钮和大面积容器不使用品牌红。
- 选中状态不能只依靠颜色，还需结合字重、下划线或图标形态。

### 1.3 双字体阅读体系

- 功能界面使用黑体，保证小字号下的识别效率。
- 文章标题和长文正文使用宋体，建立权威、沉静的编辑感。
- 数字和英文优先使用平台系统字体。

### 1.4 原生平台体验

- 遵循平台安全区、返回手势、触控尺寸和动态字体规范。
- 列表位置和阅读进度应在页面切换后保留。一期没有播放器。
- 动效只用于反馈操作和解释空间关系。

## 2. 品牌

### 2.1 产品名称

- 中文名称：知见
- 建议英文名称：Zhijian
- 品牌语义：从资讯中获得知识，从知识中形成见解。

### 2.2 Logo 使用

- Logo 应使用独立设计的官方图形资产，不使用普通字体临时排版。
- 顶栏 Logo 建议使用白色版本，置于品牌红背景。
- Logo 四周最小留白不低于 Logo 高度的 `0.25` 倍。
- 禁止拉伸、描边、添加阴影、渐变或修改标准比例。

## 3. 色彩系统

### 3.1 基础色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `color.brand.primary` | `#E9271C` | 品牌背景、选中态、关键操作 |
| `color.brand.pressed` | `#C91F18` | 品牌按钮按压态 |
| `color.brand.tint` | `#FDECEB` | 弱提示、浅红标签背景 |
| `color.text.primary` | `#222222` | 标题、正文、重要数值 |
| `color.text.secondary` | `#6F6F6F` | 副标题、来源、说明文字 |
| `color.text.tertiary` | `#A6A6A6` | 阅读量、占位符、未选图标 |
| `color.text.inverse` | `#FFFFFF` | 品牌红背景上的文字 |
| `color.surface.primary` | `#FFFFFF` | 页面主背景 |
| `color.surface.subtle` | `#F7F7F7` | 输入框、标签、弱容器 |
| `color.surface.pressed` | `#F2F2F2` | 列表和浅色控件按压态 |
| `color.border.default` | `#EEEEEE` | 分割线、弱边框 |
| `color.overlay` | `rgba(0, 0, 0, 0.45)` | 图片蒙层、模态遮罩 |

> 上表与当前实现的 CSS 变量一致。更换品牌资产时，只改令牌，不在页面里写死新色值。

### 3.2 使用比例

- 白色及浅灰表面：约 80%
- 主文字及灰阶信息：约 15%
- 品牌红及其他强调色：不超过 5%

### 3.3 可访问性

- 普通正文与背景的对比度不得低于 `4.5:1`。
- 大号文字及关键图标不得低于 `3:1`。
- 禁止仅依赖红色表达错误或选中。
- 一期不做深色模式，不提供主题切换。

## 4. 字体系统

### 4.1 字体族

```css
--font-ui: "PingFang SC", "Noto Sans CJK SC", "Microsoft YaHei", sans-serif;
--font-reading: "Songti SC", STSong, "Noto Serif CJK SC", serif;
--font-latin: "SF Pro Text", "Roboto", sans-serif;
```

平台建议：

- iOS：`PingFang SC` + `Songti SC` + `SF Pro Text`
- Android：`Noto Sans CJK SC` + `Noto Serif CJK SC` + `Roboto`
- Web：优先使用合法授权的 Noto CJK 字体，并提供系统字体回退

### 4.2 字体层级

| Token | 字体 | 字号 / 行高 | 字重 | 使用场景 |
| --- | --- | --- | --- | --- |
| `type.article.title` | Reading | `24 / 34 pt` | 700 | 文章大标题 |
| `type.article.subtitle` | Reading | `16 / 24 pt` | 400 | 副标题，使用次文字色 |
| `type.article.reads` | UI | `12 / 17 pt` | 400 | 标题下的阅读量，使用三级文字色 |
| `type.article.body` | Reading | `18 / 32 pt` | 400 | 长文正文 |
| `type.section.title` | Reading | `20 / 28 pt` | 700 | 观点、推荐阅读 |
| `type.feed.title` | UI | `18 / 27 pt` | 400 | 信息流标题 |
| `type.navigation` | UI | `16 / 22 pt` | 500 | 顶部频道导航 |
| `type.navigation.active` | UI | `16 / 22 pt` | 600 | 当前频道 |
| `type.button` | UI | `14 / 20 pt` | 500 | 登录、回复、主要操作 |
| `type.metadata` | UI | `13 / 18 pt` | 400 | 来源、日期、阅读数 |
| `type.caption` | UI | `12 / 17 pt` | 400 | 图片说明、弱提示 |

### 4.3 排版规则

- 中文正文使用全角标点，不额外增加字符间距。
- 长文正文行高约为字号的 `1.7` 倍。
- 信息流标题最多显示 3 行，超出时截断。
- 正文段落间距建议为 `20–24 pt`，首行不强制缩进。
- 重要数字可使用 `600` 字重，但不使用额外字体颜色制造噪声。
- 动态字体放大后不得遮挡、重叠或隐藏核心操作。

## 5. 布局系统

### 5.1 间距

采用 `4 pt` 基础网格：

```text
space.1 = 4
space.2 = 8
space.3 = 12
space.4 = 16
space.5 = 20
space.6 = 24
space.8 = 32
space.10 = 40
```

- 手机页面水平边距：`20–22 pt`
- 列表项垂直内边距：`16–20 pt`
- 图标与文字间距：`6–8 pt`
- 区块间距：`24–32 pt`
- 正文段落间距：`20–24 pt`

### 5.2 圆角

| Token | 数值 | 用途 |
| --- | --- | --- |
| `radius.none` | `0` | 文章、列表、导航 |
| `radius.small` | `4 pt` | 缩略图、标签 |
| `radius.medium` | `8 pt` | 输入框、缩略图、弱容器 |
| `radius.circle` | `50%` | 头像、圆形品牌入口 |

组件应保持平直、克制，避免全部使用胶囊形状。

### 5.3 安全区

- 固定顶栏必须包含系统状态栏安全区。
- 底部导航和文章操作栏必须避让 Home Indicator。
- 滚动容器需增加与固定头尾栏等高的内容 inset。
- 横屏和平板端应限制长文最大阅读宽度，建议不超过 `720 pt`。

## 6. 图标

界面图标使用 PNG，保证小程序和 H5 的边缘一致。不在正式界面混用 SVG、字体图标和临时手绘稿。

- 同一组图标来自同一套图形，线宽和留白接近。
- 默认使用 `#6F6F6F` 线框。已点赞、已收藏使用品牌红实心，并改文案，不能只变色。
- 底栏和吸底条图标约 `19 pt`，点击热区不低于 `44×44 pt`。
- 分享渠道使用微信、朋友圈、微博的标准图形，不用简化替代。单枚约 `48 pt`。
- 独立图标按钮必须提供无障碍名称。

## 7. 动效与反馈

### 7.1 时间令牌

```text
motion.instant = 80 ms
motion.fast    = 120 ms
motion.normal  = 240 ms
motion.slow    = 320 ms
```

### 7.2 动效规则

- 按压反馈在 `80–120 ms` 内出现。
- 列表按压使用浅灰背景或内容透明度变化，不改变布局尺寸。
- 页面进入时长建议 `220–280 ms`，保持平台原生方向。
- 弹层退出速度应略快于进入速度。
- 点赞和收藏先切换图标与文案，再同步结果。失败时回到原状态并说明原因。
- 支持系统“减少动态效果”设置。
- 禁止为了装饰使用持续循环动画。

## 8. 导航规范

演示和正式版的底栏不同。两套都遵守同一套高度、颜色和图标规则，不在未完成的位置放占位项。

### 8.1 首页顶栏 `AppHeader`

组成：

- 白色「知见」字标

规格：

- 品牌红背景，字标使用阅读字体
- 内容高度约 `52 pt`，不含顶部安全区
- 不放搜索框，不放头像
- 顶栏固定时不得遮挡频道导航

### 8.2 文章顶栏 `ArticleNavigation`

组成：

- 左侧返回
- 居中的品牌红「知见」

规格：

- 白底，底边 `1 px` 分割线
- 内容高度 `44 pt`，不含顶部安全区
- 不放朗读，不放更多

### 8.3 内页顶栏 `StackHeader`

用于登录、我的收藏等从底栏进入的下一层。

- 左侧返回，居中标题，右侧留白与左侧等宽
- 白底，标题使用 UI `17 / 22 pt`、`600`
- 内容高度 `44 pt`

### 8.4 频道导航 `ChannelTabs`

- 单项高度不低于 `44 pt`
- 一期展示：推荐、热点、思想、民生、制度。演示可先只露出推荐和热点
- 当前项使用品牌红、`600` 字重及 `2 pt` 下划线
- 切换频道后保留各频道列表位置
- 「推荐」只是分发频道，不出现在投稿栏目里

### 8.5 底部导航 `BottomTabBar`

- 三项，不使用五个以上的入口，也不放中心加号以外的装饰
- 内容高度 `50–56 pt`，另加底部安全区
- 默认图标和文字使用 `#6F6F6F`
- 选中项使用品牌红图标。联系我们打开弹层，不保持选中
- 演示：首页、联系我们、我的
- 正式：首页、投稿、我的
- 不出现专题、消息、搜索

## 9. 核心组件库

一期不实现 `SearchField`、`AudioAction` 和视频封面。下面不保留这些组件的现行规格。

### 9.1 信息流

`FeedTextItem`：标题、摘要、作者、时间、阅读量。整行进入详情。标题最多 3 行。行间用细分割线，不用阴影。

`FeedSideItem`：左侧标题和摘要，右侧 `4:3` 头图。图片加载前占住尺寸。没有头图时改为纯文字，不显示破图。

`FeedHeroItem`：上图下文。大图在上，标题、摘要和点赞数在下。

信息流不提供播报按钮。

### 9.2 发现区 `DiscoveryRow`

首页频道下方、信息流上方，可与轮播并存。

- 左「实时热点」、右「专题」，宽度 `6:4`
- 两侧标题都用左侧 `3–4 pt` 品牌红竖线
- 实时热点为序号列表。前三名序号使用品牌红
- 专题为三行两列，共 6 块。每块是图片，底部居中白色话题名
- 两列等高。没有专题数据时隐藏右列，不留空格子

### 9.3 文章头部 `ArticleMasthead`

从上到下：

1. 主标题，`type.article.title`
2. 副标题，仅在有内容时出现
3. 阅读量，例如 `18,642 阅读`，紧挨副标题，使用三级文字色
4. 作者行：圆形首字头像、名称、时间

规则：

- 阅读量不是文末的灰色指标卡，也不和点赞并排
- 作者行没有关注按钮
- 标题与副标题间距约 `8–10 pt`，阅读量与作者行间距约 `12–16 pt`

### 9.4 正文 `ArticleBody`

- 使用阅读字体
- 只排段落、小标题、图片、引用
- 不排视频、链接块、列表和粗体工具
- 图片不超出正文容器，失败时显示替代文字
- 段落间距 `18–24 pt`，两端对齐

### 9.5 标签 `TagChip`

- 浅灰底、次文字色、`4 pt` 圆角
- 一期只作展示，不跳进未交付的聚合页

### 9.6 评论 `CommentRow`

- 头像、名称、地区、楼层、时间、正文、点赞
- 名称使用偏暖的次要色，正文使用主文字色
- 点赞用与吸底条同一套大拇指图标
- 审核中的评论不进入公开列表
- 行间用留白或细分割线

评论未对正式用户开放时，不展示可提交的输入结果。演示可以展示静态评论。

### 9.7 吸底条 `ArticleActionDock`

从左到右：

- 评论输入，浅灰底，占位「说说你的观点」
- 评论图标和计数
- 点赞图标和计数
- 收藏图标和「收藏」或「已收藏」
- 分享图标和「分享」

规则：

- 固定在底部并避让安全区
- 未点赞、未收藏为灰色线框
- 已点赞、已收藏为品牌红实心，文字同时变为品牌红
- 每个操作热区不小于 `44×44 pt`

### 9.8 分享弹层 `ShareSheet`

- 自底部升起，遮罩 `color.overlay`
- 标题「分享到」
- 三个渠道横排：微信、朋友圈、微博。图标在上，名称在下
- 底部「取消」，与渠道区用分割线隔开
- 演示点击渠道后关闭弹层，并提示链接已复制

### 9.9 收藏行 `FavoriteRow`

- 一行一篇，左文右图
- 主标题最多 2 行，`600`；副标题最多 2 行，次文字色
- 右侧头图约 `110×78 pt`，`4:3`，圆角 `4 pt`
- 行间细分割线
- 空列表说明「还没有收藏」，并告诉用户去文章页收藏

### 9.10 个人中心

- 页面底为浅灰，内容是白底圆角块
- 已登录：品牌红首字头像、名称、邮箱或手机号
- 菜单只有实际可用项。演示为「我的收藏」「退出登录」
- 正式版在退出登录之上增加「我的投稿」
- 不出现关注、消息、搜索

### 9.11 登录

- 演示：邮箱、密码、提交。错误出现在字段附近，不用只靠红色
- 正式 H5：手机号、协议勾选、验证码。未勾选协议时，获取验证码不可用
- 顶栏使用 `StackHeader`

### 9.12 联系我们

只存在于演示底栏。

- 白底弹层，顶部一条品牌红
- 标题和两段说明
- 只保留邮箱，不出现微信
- 点击遮罩或关闭后，底栏不停留在选中态

### 9.13 分区标题 `SectionHeader`

用于「推荐阅读」等分区：

- 左侧 `3–4 pt` 品牌红竖线
- 标题使用 UI Semibold
- 区块顶部间距不低于 `24 pt`

## 10. 页面模板

### 10.1 首页

```text
系统安全区
AppHeader
ChannelTabs
热点轮播
DiscoveryRow
最新内容
BottomTabBar
底部安全区
```

没有热点时去掉轮播，不留空白。频道切换只替换信息流。

### 10.2 文章页

```text
系统安全区
ArticleNavigation
ArticleMasthead
ArticleBody
TagChip
推荐阅读
评论
ArticleActionDock
底部安全区
```

吸底条不得挡住最后一段正文或最后一条评论，页面底部要留出等量空白。

### 10.3 我的与收藏

```text
系统安全区
StackHeader 或个人中心标题
账号块
菜单或 FavoriteRow 列表
BottomTabBar（仅「我的」保留；收藏页不重复一套底栏）
```

收藏页使用返回，不用底栏。

## 11. 文案

- 按钮使用动词：「收藏」「已收藏」「退出登录」「邮箱登录」
- 阅读量写作 `18,642 阅读`，使用千分位
- 时间在正式文章头部使用 `2026-09-17 09:30` 这类完整时间
- 空状态说明原因，并给出下一步
- 不使用「功能建设中」「播放」「暂停」
- 分享成功的演示提示写明去哪个渠道，例如「链接已复制，去微信分享」

## 12. 验收清单

- [ ] 颜色引用语义令牌，次文字为 `#6F6F6F`，弱信息为 `#A6A6A6`
- [ ] 标题和正文使用阅读字体，阅读量和操作使用界面字体
- [ ] 首页顶栏没有搜索；文章顶栏没有朗读和更多
- [ ] 阅读量在主副标题下，点赞在吸底条
- [ ] 作者行没有关注
- [ ] 已点赞、已收藏同时改变图标和文字
- [ ] 分享弹层使用三个渠道的标准图形
- [ ] 收藏列表为一行一篇，左文右图
- [ ] 演示底栏是首页、联系我们、我的；正式底栏是首页、投稿、我的
- [ ] 没有搜索、消息、视频、朗读和「建设中」入口
- [ ] 顶栏、底栏和吸底条避让安全区，且不挡住正文结尾
- [ ] 图标为同一套 PNG，并带无障碍名称
