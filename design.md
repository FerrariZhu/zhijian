# 知见 Design System

> 版本：1.0  
> 适用范围：知见 App（iOS / Android）  
> 设计定位：权威、克制、内容优先。以品牌红建立识别，以高质量排版承载资讯与长文阅读。

## 1. 设计原则

### 1.1 内容优先

- 界面以白色和灰阶为主，将注意力让给标题、正文及新闻图片。
- 避免大面积装饰、渐变、阴影和高饱和背景。
- 使用排版、留白和细分割线建立信息层级。

### 1.2 红色表达状态

- 品牌红仅用于品牌区域、选中状态、关键操作和重点栏目。
- 普通正文、次要按钮和大面积容器不使用品牌红。
- 选中状态不能只依靠颜色，还需结合字重、下划线或图标形态。

### 1.3 双字体阅读体系

- 功能界面使用黑体，保证小字号下的识别效率。
- 文章标题和长文正文使用宋体，建立权威、沉静的编辑感。
- 数字和英文优先使用平台系统字体。

### 1.4 原生平台体验

- 遵循平台安全区、返回手势、触控尺寸和动态字体规范。
- 列表位置、阅读进度及播放状态应在页面切换后保留。
- 动效只用于反馈操作和解释空间关系。

## 2. 品牌

### 2.1 产品名称

- 中文名称：知见
- 建议英文名称：Zhijian
- 品牌语义：从资讯中获得知识，从知识中形成见解。

### 2.2 Logo 使用

- Logo 应使用独立设计的官方图形资产，不使用普通字体临时排版。
- 顶栏 Logo 建议使用白色版本，置于品牌红背景。
- Logo 四周最小留白不低于 Logo 高度的 `0.25` 倍。
- 禁止拉伸、描边、添加阴影、渐变或修改标准比例。

## 3. 色彩系统

### 3.1 基础色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `color.brand.primary` | `#E9271C` | 品牌背景、选中态、关键操作 |
| `color.brand.pressed` | `#C91F18` | 品牌按钮按压态 |
| `color.brand.tint` | `#FDECEB` | 弱提示、浅红标签背景 |
| `color.text.primary` | `#222222` | 标题、正文、重要数值 |
| `color.text.secondary` | `#7A7A7A` | 来源、日期、说明文字 |
| `color.text.tertiary` | `#B5B5B5` | 占位符、未选图标、弱信息 |
| `color.text.inverse` | `#FFFFFF` | 品牌红背景上的文字 |
| `color.surface.primary` | `#FFFFFF` | 页面主背景 |
| `color.surface.subtle` | `#F7F7F7` | 输入框、标签、弱容器 |
| `color.surface.pressed` | `#F2F2F2` | 列表和浅色控件按压态 |
| `color.border.default` | `#EEEEEE` | 分割线、弱边框 |
| `color.overlay` | `rgba(0, 0, 0, 0.45)` | 图片蒙层、模态遮罩 |

> 品牌红来自截图近似值。上线前应使用最终品牌资产重新取样，并统一更新语义令牌。

### 3.2 使用比例

- 白色及浅灰表面：约 80%
- 主文字及灰阶信息：约 15%
- 品牌红及其他强调色：不超过 5%

### 3.3 可访问性

- 普通正文与背景的对比度不得低于 `4.5:1`。
- 大号文字及关键图标不得低于 `3:1`。
- 禁止仅依赖红色表达错误、选中或播放状态。
- 深色模式需单独建立令牌映射，不直接反转浅色模式。

## 4. 字体系统

### 4.1 字体族

```css
--font-ui: "PingFang SC", "Noto Sans CJK SC", "Microsoft YaHei", sans-serif;
--font-reading: "Songti SC", STSong, "Noto Serif CJK SC", serif;
--font-latin: "SF Pro Text", "Roboto", sans-serif;
```

平台建议：

- iOS：`PingFang SC` + `Songti SC` + `SF Pro Text`
- Android：`Noto Sans CJK SC` + `Noto Serif CJK SC` + `Roboto`
- Web：优先使用合法授权的 Noto CJK 字体，并提供系统字体回退

### 4.2 字体层级

| Token | 字体 | 字号 / 行高 | 字重 | 使用场景 |
| --- | --- | --- | --- | --- |
| `type.article.title` | Reading | `22 / 32 pt` | 700 | 文章大标题 |
| `type.article.body` | Reading | `18 / 31 pt` | 400 | 长文正文 |
| `type.section.title` | Reading | `20 / 28 pt` | 700 | 观点、推荐阅读 |
| `type.feed.title` | UI | `18 / 27 pt` | 400 | 信息流标题 |
| `type.navigation` | UI | `16 / 22 pt` | 500 | 顶部频道导航 |
| `type.navigation.active` | UI | `16 / 22 pt` | 600 | 当前频道 |
| `type.button` | UI | `14 / 20 pt` | 500 | 播报、回复、操作按钮 |
| `type.metadata` | UI | `13 / 18 pt` | 400 | 来源、日期、阅读数 |
| `type.caption` | UI | `12 / 17 pt` | 400 | 图片说明、弱提示 |

### 4.3 排版规则

- 中文正文使用全角标点，不额外增加字符间距。
- 长文正文行高约为字号的 `1.7` 倍。
- 信息流标题最多显示 3 行，超出时截断。
- 正文段落间距建议为 `20–24 pt`，首行不强制缩进。
- 重要数字可使用 `600` 字重，但不使用额外字体颜色制造噪声。
- 动态字体放大后不得遮挡、重叠或隐藏核心操作。

## 5. 布局系统

### 5.1 间距

采用 `4 pt` 基础网格：

```text
space.1 = 4
space.2 = 8
space.3 = 12
space.4 = 16
space.5 = 20
space.6 = 24
space.8 = 32
space.10 = 40
```

- 手机页面水平边距：`20–22 pt`
- 列表项垂直内边距：`16–20 pt`
- 图标与文字间距：`6–8 pt`
- 区块间距：`24–32 pt`
- 正文段落间距：`20–24 pt`

### 5.2 圆角

| Token | 数值 | 用途 |
| --- | --- | --- |
| `radius.none` | `0` | 文章、列表、导航 |
| `radius.small` | `4 pt` | 缩略图、标签 |
| `radius.medium` | `8 pt` | 搜索框、弱容器 |
| `radius.circle` | `50%` | 头像、圆形品牌入口 |

组件应保持平直、克制，避免全部使用胶囊形状。

### 5.3 安全区

- 固定顶栏必须包含系统状态栏安全区。
- 底部导航和文章操作栏必须避让 Home Indicator。
- 滚动容器需增加与固定头尾栏等高的内容 inset。
- 横屏和平板端应限制长文最大阅读宽度，建议不超过 `720 pt`。

## 6. 图标

- 使用同一套 `1.5–2 pt` 描边的矢量图标。
- 同一导航层级不得混用实心和线框风格。
- 常规图标：`22–24 pt`
- 小型辅助图标：`16–18 pt`
- 点击热区：iOS 不低于 `44×44 pt`，Android 不低于 `48×48 dp`
- 独立图标按钮必须提供无障碍名称。
- 装饰图标不进入无障碍朗读顺序。

## 7. 动效与反馈

### 7.1 时间令牌

```text
motion.instant = 80 ms
motion.fast    = 120 ms
motion.normal  = 240 ms
motion.slow    = 320 ms
```

### 7.2 动效规则

- 按压反馈在 `80–120 ms` 内出现。
- 列表按压使用浅灰背景或内容透明度变化，不改变布局尺寸。
- 页面进入时长建议 `220–280 ms`，保持平台原生方向。
- 弹层退出速度应略快于进入速度。
- 播放、点赞、收藏操作先反馈状态，再执行非阻塞网络同步。
- 支持系统“减少动态效果”设置。
- 禁止为了装饰使用持续循环动画。

## 8. 导航规范

### 8.1 顶部品牌栏 `AppHeader`

组成：

- 品牌 Logo
- 搜索入口
- 用户入口或快捷操作

规格：

- 内容高度：`56 pt`，不含顶部安全区
- 品牌红背景，文字和图标使用白色
- 搜索框使用低透明白色或浅色容器
- 顶栏固定时不得遮挡频道导航

### 8.2 频道导航 `ChannelTabs`

- 单项高度不低于 `44 pt`
- 支持横向滚动
- 当前项使用品牌红、`600` 字重及 `2 pt` 下划线
- 切换频道后保留各频道列表位置
- 选中状态需向屏幕阅读器明确播报

### 8.3 底部导航 `BottomTabBar`

- 导航项不超过 5 个
- 内容高度建议 `50–56 pt`，另加底部安全区
- 默认图标和文字使用弱灰色
- 选中项使用品牌红或明确的品牌图形
- 中心入口可提高视觉权重，但点击区不得侵入相邻项目

## 9. 核心组件库

### 9.1 `SearchField`

属性：

- `placeholder`
- `value`
- `onSearch`
- `loading`

状态：

- Default
- Focused
- Typing
- Loading
- Disabled

规格：

- 高度 `36–40 pt`
- 圆角 `8 pt`
- 左侧搜索图标，右侧可放清除或筛选操作

### 9.2 `TopicChips`

属性：

- `items`
- `selectedId`
- `onSelect`

状态：

- Default
- Selected
- Pressed
- Disabled

规格：

- 高度约 `34 pt`
- 使用 `4 pt` 小圆角
- 选中态采用品牌红背景和白色文字

### 9.3 `FeedTextItem`

属性：

- `title`
- `source`
- `date`
- `audioState`
- `onPress`

规则：

- 整个列表项可进入详情。
- 播报按钮为独立操作，事件不得冒泡进入详情。
- 标题最多 3 行。
- 使用细分割线，不使用卡片阴影。

### 9.4 `FeedMediaItem`

变体：

- `SingleImage`
- `TrailingImage`
- `Video`

属性：

- `title`
- `thumbnail`
- `duration`
- `source`
- `date`

规则：

- 普通缩略图建议采用 `4:3`。
- 视频封面叠加播放图标和右下角时长。
- 图片加载前保留固定尺寸，避免列表跳动。

### 9.5 `AudioAction`

状态：

- Idle
- Loading
- Playing
- Paused
- Error

规则：

- 品牌红图标与文字组合。
- 点击区不小于 `44×44 pt`。
- 播放时必须持续显示状态，并允许暂停。
- 页面离开后是否继续播放应由统一播放器策略决定。

### 9.6 `ArticleHeader`

组成：

- 文章标题
- 来源
- 发布日期

规则：

- 标题使用 `type.article.title`。
- 标题与元数据间距建议 `20–24 pt`。
- 来源使用次文字色，不与正文争夺注意力。

### 9.7 `ArticleBody`

- 使用阅读字体和 `type.article.body`。
- 支持段落、图片、引用、小标题、视频及相关链接。
- 图片不得超出正文容器。
- 正文选择、朗读和动态字体不能破坏布局。

### 9.8 `TagChip`

- 使用浅灰背景和 UI 字体。
- 可使用小型品牌红频道标识。
- 整体可点击，点击后进入聚合页。
- 标签不使用高饱和整块背景。

### 9.9 `MetricsBar`

展示：

- 阅读量
- 点赞量
- 其他核心统计

规则：

- 使用浅灰容器。
- 数值使用 `600` 字重。
- 点赞后立即切换状态并更新计数。

### 9.10 `CommentRow`

属性：

- `avatar`
- `name`
- `content`
- `time`
- `likes`
- `replyCount`

规则：

- 用户名使用次要暖灰或品牌辅助色，正文使用主文字色。
- 点赞与更多操作弱化显示。
- 点击“回复”后聚焦输入框，并明确当前回复对象。
- 评论列表之间使用留白或细分割线。

### 9.11 `ArticleActionDock`

组成：

- 评论输入入口
- 评论数
- 收藏
- 分享

规则：

- 固定在底部并避让安全区。
- 每个操作点击区不小于 `44×44 pt`。
- 收藏、点赞等选中状态同时改变图形和文字描述。

### 9.12 `SectionHeader`

用于“观点”“推荐阅读”等内容分区：

- 左侧使用 `3–4 pt` 品牌红竖线。
- 标题使用宋体 Bold 或 UI Semibold，但同一页面必须统一。
- 区块顶部间距不低于 `24 pt`。

## 10. 页面模板

### 10.1 信息流页

```text
系统安全区
AppHeader
ChannelTabs
TopicChips（可选）
Feed List
BottomTabBar
底部安全区
```

### 10.2 文章页

```text
系统安全区
ArticleNavigation
ArticleHeader
ArticleBody
Tags
MetricsBar
RelatedContent
Comments
ArticleActionDock
底部安全区
```

## 11. 内容与文案

- 标题应准确、完整，避免纯情绪化表达。
- 按钮优先使用动词，例如“播放”“暂停”“回复”“收藏”。
- 时间统一使用产品约定格式，例如 `2026-09-16`。
- 大数字使用千分位，例如 `115,598`。
- 加载失败需说明原因，并提供“重新加载”操作。
- 空状态必须给出下一步，而非只显示“暂无内容”。

## 12. 验收清单

- [ ] 所有颜色均引用语义令牌，无页面级散落色值。
- [ ] 功能文字与长文阅读正确使用对应字体。
- [ ] 普通正文对比度不低于 `4.5:1`。
- [ ] 所有点击区域满足平台最小触控尺寸。
- [ ] 顶栏、底栏和滚动内容正确避让安全区。
- [ ] 列表图片已预留尺寸，不产生布局跳动。
- [ ] 播放、收藏、点赞均有即时状态反馈。
- [ ] 选中和错误状态不只依赖颜色。
- [ ] 支持动态字体和系统减少动态效果。
- [ ] 小屏、横屏、大屏及平板阅读宽度均已验证。
- [ ] 固定操作栏不会遮挡最后一段正文或评论。
- [ ] 图标来自同一矢量图标体系，并具备无障碍名称。

