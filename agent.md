# 知见 · Agent 约定

改产品行为或界面之前，先读 [PRD.md](./PRD.md) 和 [design.md](./design.md)。两者冲突时，范围以 PRD v1.2 为准，颜色、字体、组件规格以 design.md 1.1 为准。

`PRD-补充版.md`、`checklist.md` 是历史输入。和现行 PRD 不一致时，不要按旧稿把功能加回来。

## 现在在做什么

这是 Taro 4 + React + TypeScript 的微信小程序 / H5 演示。数据在 `src/data/content.ts`，没有后端。

`npm run dev:h5` 后打开 `http://localhost:10086`。`app.config.ts` 里新增页面不会热更新，必须重启开发服务，并给地址加查询参数避免浏览器吃旧包。

H5 会留下隐藏的页面栈。点页面元素时，先找到宽度大于 0 的 `.taro_page`，不要点到藏在下面的那一层。

## 演示和正式版不要混

演示可以继续用现有页面讲产品。正式环境在内容定位没有书面结论前，只做阅读，不接真实评论、真实投稿和真实账号。

| | 演示（当前代码） | 正式一期 |
|---|---|---|
| 登录 | 仅邮箱和密码，密码不落盘 | 手机号验证码。不要把演示改成手机号，除非用户明确要求 |
| 底栏 | 首页、联系我们、我的 | 首页、投稿、我的 |
| 个人中心 | 账号、我的收藏、退出登录 | 再加我的投稿。不加关注 |
| 分享 | 复制链接，渠道是微信、朋友圈、微博 | 小程序走微信分享。H5 不承诺每篇一张分享图 |

联系我们只展示邮箱，不是投诉渠道。

## 改界面时

- 图标用 PNG，放在 `src/assets/icons/`。不要用 SVG，也不要再手绘一套线框充数。默认灰 `#6F6F6F`，已点赞和已收藏用品牌红实心，文字一起变。
- 颜色只用 `src/app.scss` 里的变量。品牌红 `#E9271C`。不加渐变和装饰阴影。
- 文章顶栏只有返回和居中的「知见」。阅读量放在主副标题下面。点赞和计数在吸底条。作者行没有关注。
- 收藏列表一行一篇：左边主副标题，右边头图。
- 首页实时热点和专题是 6:4，专题 6 块、三行两列。
- 未做的搜索、消息、朗读、视频、字号，不要加按钮，也不要弹「建设中」。
- 正文正式渲染只保留段落、小标题、图片、引用。不要按第二段写死插图。

## 还没接通、不要当成产品规则

- 详情页目前忽略路由里的文章编号，始终打开同一篇。下次改详情或跳转时，要按 `id` 打开对应文章。热点编号和文章编号也不对齐，链接只能指向真实文章。
- 评论是静态数据。正式版默认先审后发，不要做成提交后立刻公开。
- 投稿页的 AI 是前端演示。正式版 AI 默认关闭，结果必须用户确认后才写入，不能自动提交。

## 本地状态

- 登录：`zhijian-email-session`，只存邮箱字符串。
- 收藏：`zhijian-favorites`，存文章编号数组。取消收藏后列表里必须消失。

## 做完要看

改了页面、样式、路由或展示数据，就在浏览器里走一遍真实操作，不要只看截图。相关页面一起看，例如收藏按钮和个人中心的收藏列表。

没有用户要求时，不要提交，也不要改 git 配置。
