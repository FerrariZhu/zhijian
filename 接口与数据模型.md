# 知见一期接口与数据模型

> 版本：v1.0  
> 适用客户端：微信小程序、H5  
> 对应页面：首页、内容详情页、投稿页  
> 一期约束：不建设运营配置后台；运营内容由可信 SQL、导入脚本或内部任务直接写数据库。

## 1. 总体架构

```text
小程序 / H5
    │
    ▼
业务 API
    ├── 内容查询
    ├── 评论与互动
    ├── 投稿草稿
    ├── AI 成文 / 改写
    └── 上传凭证
    │
    ├────────► PostgreSQL
    ├────────► 对象存储 + CDN
    └────────► AI 服务

运营人员 / 内容导入任务
    │
    ├── 上传图片至对象存储
    └── 通过受控 SQL / 脚本写 PostgreSQL
```

推荐技术：

- 主数据库：PostgreSQL 16+
- 图片与附件：对象存储（腾讯云 COS、阿里云 OSS 或 S3）+ CDN
- API：REST JSON，统一前缀 `/api/v1`
- 身份认证：微信小程序登录 + H5 手机号或微信 OAuth
- Redis：一期可选；热点首页、计数和登录会话量上来后再增加

### 1.1 “内容直接写库”的边界

“直接写库”只适用于运营内容导入，不表示客户端直连数据库。

- 小程序和 H5 只能访问业务 API。
- 数据库端口、账号和密码不能进入前端包。
- 运营写库应使用独立的 `content_writer` 数据库角色。
- `content_writer` 仅允许写内容、媒体、标签、推荐和轮播相关表。
- 用户、评论、互动、投稿必须通过 API 写入，避免越权和脏数据。
- 生产写库脚本应先写 `DRAFT`，完成关联数据后在同一事务内改为 `PUBLISHED`。

## 2. 通用接口约定

### 2.1 请求

```http
Base URL: https://api.example.com/api/v1
Authorization: Bearer <access_token>
Content-Type: application/json
X-Request-Id: <uuid>
X-Platform: weapp | h5
```

公开内容允许匿名读取；点赞、收藏、评论、投稿及 AI 功能必须登录。

### 2.2 时间与 ID

- 所有 ID 使用 UUID 字符串。
- API 时间统一返回 ISO 8601 UTC，例如 `2026-09-17T01:30:00Z`。
- “12 分钟前”“今天 09:30”由前端根据 ISO 时间格式化。
- 数据库存储使用 `timestamptz`。

### 2.3 标准响应

成功：

```json
{
  "code": "OK",
  "message": "",
  "data": {},
  "request_id": "66f6aa85-14e6-4dfa-87cc-8c48e0f58315"
}
```

失败：

```json
{
  "code": "CONTENT_NOT_FOUND",
  "message": "内容不存在或已下线",
  "details": null,
  "request_id": "66f6aa85-14e6-4dfa-87cc-8c48e0f58315"
}
```

建议错误码：

- `UNAUTHORIZED`
- `FORBIDDEN`
- `VALIDATION_ERROR`
- `CONTENT_NOT_FOUND`
- `COMMENT_NOT_FOUND`
- `SUBMISSION_NOT_FOUND`
- `UPLOAD_FAILED`
- `AI_TASK_FAILED`
- `RATE_LIMITED`
- `INTERNAL_ERROR`

### 2.4 分页

信息流和评论采用游标分页，不采用页码偏移：

```json
{
  "items": [],
  "next_cursor": "eyJwdWJsaXNoZWRfYXQiOiIyMDI2...",
  "has_more": true
}
```

## 3. 页面消费字段

## 3.1 首页

### 频道导航

接口：`GET /home`

消费字段：

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `channels[].id` | UUID | 频道唯一标识 |
| `channels[].key` | string | 请求信息流时使用的稳定键 |
| `channels[].name` | string | 推荐、热点、思想等显示名称 |
| `channels[].sort_order` | integer | 导航排序 |

### 顶部热点轮播

接口：`GET /home`

规则：

- 最多返回 5 条。
- 按 `position ASC` 排序。
- 客户端每 5 秒自动切换并支持手动滑动。
- API 只返回有效时间范围内、状态为 `PUBLISHED` 的内容。

消费字段：

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `carousel[].id` | UUID | 轮播记录 ID |
| `carousel[].content_id` | UUID | 点击进入详情 |
| `carousel[].title` | string | 轮播主标题 |
| `carousel[].image.url` | string | 轮播 PNG/JPEG 图片 |
| `carousel[].image.width` | integer | 图片原始宽度 |
| `carousel[].image.height` | integer | 图片原始高度 |
| `carousel[].image.alt` | string | 无障碍图片说明 |
| `carousel[].position` | integer | 轮播顺序，1–5 |

计数器不需要接口单独返回：

```text
当前序号 = 当前轮播数组下标 + 1
总数 = carousel.length
```

### 最新内容信息流

接口：`GET /home?channel_key=recommend&cursor=&limit=20`

消费字段：

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `feed.items[].id` | UUID | 内容 ID |
| `feed.items[].title` | string | 主标题 |
| `feed.items[].summary` | string | 摘要 |
| `feed.items[].author.name` | string | 作者名称 |
| `feed.items[].channel.name` | string | 栏目名称 |
| `feed.items[].published_at` | datetime | 发布时间 |
| `feed.items[].cover` | Media/null | 列表图片 |
| `feed.items[].display_variant` | enum | `TEXT`、`SIDE_IMAGE`、`HERO_IMAGE` |
| `feed.items[].stats.read_count` | integer | 阅读数 |
| `feed.items[].stats.like_count` | integer | 点赞数 |

## 3.2 内容详情页

### 正文

接口：`GET /contents/{content_id}`

消费字段：

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `id` | UUID | 内容 ID |
| `title` | string | 主标题 |
| `subtitle` | string/null | 副标题 |
| `summary` | string/null | 分享摘要、列表摘要 |
| `hook` | string/null | 正文末尾重点问题 |
| `author.id` | UUID | 作者 ID |
| `author.name` | string | 发布人名称 |
| `author.avatar_url` | string/null | 发布人头像 |
| `published_at` | datetime | 发布时间 |
| `hero_image` | Media/null | 正文主图 |
| `body_blocks` | Block[] | 正文结构化内容 |
| `tags[]` | Tag[] | 标签 |
| `stats.read_count` | integer | 阅读数 |
| `stats.like_count` | integer | 点赞数 |
| `stats.comment_count` | integer | 评论数 |
| `viewer.liked` | boolean | 当前用户是否点赞 |
| `viewer.favorited` | boolean | 当前用户是否收藏 |
| `viewer.following_author` | boolean | 当前用户是否关注作者 |

`body_blocks` 一期支持：

```json
[
  {
    "type": "paragraph",
    "text": "正文段落"
  },
  {
    "type": "heading",
    "level": 2,
    "text": "小标题"
  },
  {
    "type": "image",
    "asset_id": "3ee0515b-15ea-4dd5-8634-b72a5d7e7653",
    "url": "https://cdn.example.com/content/example.png",
    "width": 1280,
    "height": 720,
    "alt": "两位学者在会议室讨论"
  },
  {
    "type": "quote",
    "text": "引用内容",
    "source": "来源"
  }
]
```

正文同时保存 `body_text`，用于检索、审核和 AI 处理；前端只消费 `body_blocks`。

### 推荐阅读

接口：`GET /contents/{content_id}/recommendations?limit=3`

消费字段与首页信息流条目一致。

推荐顺序：

1. 优先读取 `content_recommendations` 人工写库关系。
2. 不足 3 条时按相同频道、共同标签和发布时间自动补齐。
3. 排除当前内容和已下线内容。

### 评论与楼层

接口：`GET /contents/{content_id}/comments?cursor=&limit=20`

消费字段：

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `items[].id` | UUID | 评论 ID |
| `items[].floor_no` | integer | 楼层 |
| `items[].user.id` | UUID | 评论人 ID |
| `items[].user.name` | string | 评论人名称 |
| `items[].user.avatar_url` | string/null | 评论人头像 |
| `items[].user.region` | string/null | 评论人地区 |
| `items[].body` | string | 评论内容 |
| `items[].created_at` | datetime | 评论时间 |
| `items[].like_count` | integer | 点赞数 |
| `items[].viewer_liked` | boolean | 当前用户是否点赞 |
| `items[].reply_count` | integer | 回复数量 |
| `items[].preview_replies[]` | Reply[] | 默认展示的 1–2 条回复 |

### 详情页互动

| 功能 | 方法与接口 |
| --- | --- |
| 记录阅读 | `POST /contents/{id}/read` |
| 点赞内容 | `PUT /contents/{id}/like` |
| 取消点赞 | `DELETE /contents/{id}/like` |
| 收藏内容 | `PUT /contents/{id}/favorite` |
| 取消收藏 | `DELETE /contents/{id}/favorite` |
| 关注作者 | `PUT /authors/{id}/follow` |
| 取消关注 | `DELETE /authors/{id}/follow` |
| 发表评论 | `POST /contents/{id}/comments` |
| 回复评论 | `POST /comments/{comment_id}/replies` |
| 点赞评论 | `PUT /comments/{comment_id}/like` |
| 取消评论点赞 | `DELETE /comments/{comment_id}/like` |

发表评论：

```json
{
  "body": "观点不同可以坐在一起交流。",
  "client_request_id": "41e50dbe-f340-4a79-9dad-8a9342a5e80a"
}
```

回复评论：

```json
{
  "body": "把分歧说清楚，是形成共识的第一步。",
  "reply_to_user_id": "60a24338-8d26-4e86-9752-a257fd28bc35",
  "client_request_id": "08267315-6051-4d61-97bd-5fa1867a4194"
}
```

`client_request_id` 用于防止用户重复点击造成重复评论。

## 3.3 投稿页

### 上传图片

推荐客户端直传对象存储，避免图片流量穿过业务 API。

第一步：

```http
POST /uploads/presign
```

```json
{
  "purpose": "SUBMISSION_COVER",
  "filename": "scene.png",
  "mime_type": "image/png",
  "size_bytes": 834221
}
```

返回：

```json
{
  "asset_id": "3ee0515b-15ea-4dd5-8634-b72a5d7e7653",
  "upload_url": "https://object-storage.example.com/...",
  "headers": {
    "Content-Type": "image/png"
  },
  "expires_at": "2026-09-17T02:00:00Z"
}
```

第二步：客户端上传成功后确认。

```http
POST /uploads/{asset_id}/complete
```

### 拍照一键成文

```http
POST /ai/photo-to-article
```

请求：

```json
{
  "asset_ids": [
    "3ee0515b-15ea-4dd5-8634-b72a5d7e7653"
  ],
  "channel_id": "985c6a20-ed9a-4acd-8015-91e18300c644",
  "instruction": "根据现场照片生成客观、克制的资讯初稿"
}
```

返回异步任务：

```json
{
  "task_id": "349d8453-d1cd-4dbf-914b-919d4db6b0a0",
  "status": "PENDING"
}
```

查询：

```http
GET /ai/tasks/{task_id}
```

完成结果：

```json
{
  "task_id": "349d8453-d1cd-4dbf-914b-919d4db6b0a0",
  "status": "SUCCEEDED",
  "result": {
    "title": "镜头里的现场：从一次相遇开始记录",
    "subtitle": "从人物、空间与细节中还原正在发生的故事",
    "body_blocks": [
      {
        "type": "paragraph",
        "text": "照片记录下一个正在发生的现场。"
      }
    ],
    "suggested_tags": ["现场", "公共讨论"]
  }
}
```

AI 结果不得自动覆盖用户正文。前端应展示结果，由用户确认后再写入编辑器。

### AI 智能改写

```http
POST /ai/rewrite
```

请求：

```json
{
  "target": "BODY",
  "mode": "POLISH",
  "title": "一场两岸民间对话实验",
  "subtitle": "当分歧被正式写在桌面上",
  "body_blocks": [
    {
      "type": "paragraph",
      "text": "原始正文"
    }
  ],
  "instruction": "保持事实不变，优化结构和表达"
}
```

`target`：

- `TITLE`
- `SUBTITLE`
- `BODY`
- `ALL`

`mode`：

- `POLISH`：润色
- `SHORTEN`：压缩
- `EXPAND`：扩写
- `RESTRUCTURE`：调整结构

### 创建草稿

```http
POST /submissions
```

请求：

```json
{
  "title": "一场两岸民间对话实验",
  "subtitle": "当分歧被正式写在桌面上，真正的讨论才刚刚开始",
  "cover_asset_id": "3ee0515b-15ea-4dd5-8634-b72a5d7e7653",
  "channel_id": "985c6a20-ed9a-4acd-8015-91e18300c644",
  "tag_ids": [
    "b93b3369-8d8b-4a88-b439-07581c5a506c"
  ],
  "body_blocks": [
    {
      "type": "paragraph",
      "text": "正文内容"
    }
  ],
  "is_original": true
}
```

保存草稿：

```http
PATCH /submissions/{submission_id}
```

提交审核：

```http
POST /submissions/{submission_id}/submit
```

一期即使没有审核后台，也不能由投稿接口直接写入已发布内容。建议状态流转：

```text
DRAFT → SUBMITTED → APPROVED → PUBLISHED
                  ↘ REJECTED
```

运营人员可用 SQL 将 `SUBMITTED` 改为 `APPROVED`，再执行发布脚本生成正式内容。

## 4. 核心返回模型

### 4.1 Media

```ts
interface Media {
  id: string
  url: string
  thumbnail_url: string | null
  mime_type: 'image/png' | 'image/jpeg' | 'image/webp'
  width: number
  height: number
  size_bytes: number
  alt: string
}
```

### 4.2 首页

```ts
interface HomeResponse {
  channels: Array<{
    id: string
    key: string
    name: string
    sort_order: number
  }>
  carousel: Array<{
    id: string
    content_id: string
    title: string
    image: Media
    position: number
  }>
  feed: {
    items: ContentListItem[]
    next_cursor: string | null
    has_more: boolean
  }
}
```

### 4.3 信息流条目

```ts
type DisplayVariant = 'TEXT' | 'SIDE_IMAGE' | 'HERO_IMAGE'

interface ContentListItem {
  id: string
  title: string
  summary: string | null
  author: {
    id: string
    name: string
  }
  channel: {
    id: string
    key: string
    name: string
  }
  published_at: string
  cover: Media | null
  display_variant: DisplayVariant
  stats: {
    read_count: number
    like_count: number
    comment_count: number
  }
}
```

## 5. API 清单

### 5.1 公开读取

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/home` | 首页频道、热点轮播及首屏信息流 |
| GET | `/contents` | 按频道查询更多信息流 |
| GET | `/contents/{id}` | 内容详情 |
| GET | `/contents/{id}/recommendations` | 推荐阅读 |
| GET | `/contents/{id}/comments` | 评论列表 |
| GET | `/comments/{id}/replies` | 评论回复列表 |

### 5.2 登录用户互动

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/contents/{id}/read` | 记录有效阅读 |
| PUT/DELETE | `/contents/{id}/like` | 点赞或取消 |
| PUT/DELETE | `/contents/{id}/favorite` | 收藏或取消 |
| PUT/DELETE | `/authors/{id}/follow` | 关注或取消 |
| POST | `/contents/{id}/comments` | 发表评论 |
| POST | `/comments/{id}/replies` | 回复评论 |
| PUT/DELETE | `/comments/{id}/like` | 评论点赞或取消 |

### 5.3 投稿与 AI

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/uploads/presign` | 获取对象存储上传凭证 |
| POST | `/uploads/{id}/complete` | 确认上传完成 |
| POST | `/ai/photo-to-article` | 拍照一键成文 |
| POST | `/ai/rewrite` | AI 改写 |
| GET | `/ai/tasks/{id}` | 查询异步 AI 任务 |
| POST | `/submissions` | 创建草稿 |
| GET | `/submissions/{id}` | 获取草稿 |
| PATCH | `/submissions/{id}` | 保存草稿 |
| POST | `/submissions/{id}/submit` | 提交审核 |

### 5.4 登录

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/auth/weapp/login` | 使用微信 `code` 换取业务会话 |
| POST | `/auth/h5/sms/send` | 发送 H5 手机验证码 |
| POST | `/auth/h5/sms/login` | 手机验证码登录 |
| POST | `/auth/refresh` | 刷新访问令牌 |
| POST | `/auth/logout` | 退出登录 |

## 6. 建议底表

## 6.1 `users`

用户账号。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `status` | varchar(20) | `ACTIVE`、`BANNED`、`DELETED` |
| `display_name` | varchar(50) | 显示名称 |
| `avatar_asset_id` | uuid | FK → media_assets，可空 |
| `region` | varchar(50) | 可空 |
| `phone_hash` | varchar(128) | H5 登录使用，可空 |
| `created_at` | timestamptz | 非空 |
| `updated_at` | timestamptz | 非空 |

## 6.2 `user_identities`

微信 OpenID、UnionID、手机号等身份映射。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `user_id` | uuid | FK → users |
| `provider` | varchar(20) | `WEAPP`、`WECHAT_H5`、`PHONE` |
| `provider_subject` | varchar(128) | OpenID 或哈希后的手机号 |
| `union_id` | varchar(128) | 可空 |
| `created_at` | timestamptz | 非空 |

唯一索引：`(provider, provider_subject)`。

## 6.3 `authors`

发布主体，可关联用户，也可表示“知见编辑部”等机构作者。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `user_id` | uuid | FK → users，可空 |
| `name` | varchar(80) | 非空 |
| `bio` | varchar(500) | 可空 |
| `avatar_asset_id` | uuid | FK → media_assets，可空 |
| `status` | varchar(20) | `ACTIVE`、`DISABLED` |
| `created_at` | timestamptz | 非空 |

## 6.4 `channels`

首页频道和投稿栏目。没有配置后台时直接维护该表。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `key` | varchar(40) | 唯一稳定键，如 `recommend` |
| `name` | varchar(40) | 显示名称 |
| `sort_order` | integer | 排序 |
| `is_visible` | boolean | 是否在客户端显示 |
| `created_at` | timestamptz | 非空 |
| `updated_at` | timestamptz | 非空 |

## 6.5 `media_assets`

图片和附件元数据，二进制文件不进入数据库。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `storage_key` | varchar(500) | 对象存储 Key，唯一 |
| `url` | varchar(1000) | CDN URL |
| `thumbnail_url` | varchar(1000) | 可空 |
| `mime_type` | varchar(100) | PNG、JPEG、WebP 等 |
| `width` | integer | 图片宽 |
| `height` | integer | 图片高 |
| `size_bytes` | bigint | 文件大小 |
| `alt_text` | varchar(300) | 图片说明 |
| `sha256` | char(64) | 去重与校验 |
| `status` | varchar(20) | `UPLOADING`、`READY`、`FAILED` |
| `created_by` | uuid | FK → users，可空 |
| `created_at` | timestamptz | 非空 |

## 6.6 `contents`

正式内容主表。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `channel_id` | uuid | FK → channels |
| `author_id` | uuid | FK → authors |
| `title` | varchar(200) | 非空 |
| `subtitle` | varchar(300) | 可空 |
| `summary` | varchar(500) | 可空 |
| `hook` | varchar(500) | 可空 |
| `cover_asset_id` | uuid | FK → media_assets，可空 |
| `hero_asset_id` | uuid | FK → media_assets，可空 |
| `display_variant` | varchar(20) | `TEXT`、`SIDE_IMAGE`、`HERO_IMAGE` |
| `body_blocks` | jsonb | 正文结构 |
| `body_text` | text | 检索、审核和 AI 使用 |
| `source_name` | varchar(100) | 可空 |
| `source_url` | varchar(1000) | 可空 |
| `is_original` | boolean | 是否原创 |
| `status` | varchar(20) | `DRAFT`、`PUBLISHED`、`OFFLINE` |
| `published_at` | timestamptz | 可空 |
| `created_at` | timestamptz | 非空 |
| `updated_at` | timestamptz | 非空 |

关键索引：

```sql
CREATE INDEX idx_contents_channel_publish
ON contents(channel_id, published_at DESC, id DESC)
WHERE status = 'PUBLISHED';

CREATE INDEX idx_contents_publish
ON contents(published_at DESC, id DESC)
WHERE status = 'PUBLISHED';
```

## 6.7 `content_stats`

内容聚合计数，避免列表查询扫描互动明细。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `content_id` | uuid | PK、FK → contents |
| `read_count` | bigint | 默认 0 |
| `like_count` | bigint | 默认 0 |
| `favorite_count` | bigint | 默认 0 |
| `comment_count` | bigint | 默认 0 |
| `updated_at` | timestamptz | 非空 |

一期可由事务内原子更新维护；流量增大后改为事件聚合。

## 6.8 `home_carousel`

首页热点轮播配置。虽然没有后台，仍建议独立建表，不在 `contents` 中增加五个固定槽位字段。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `content_id` | uuid | FK → contents |
| `image_asset_id` | uuid | FK → media_assets；可覆盖内容封面 |
| `title_override` | varchar(200) | 可空 |
| `position` | smallint | 1–5 |
| `starts_at` | timestamptz | 生效时间 |
| `ends_at` | timestamptz | 失效时间，可空 |
| `is_enabled` | boolean | 默认 true |
| `created_at` | timestamptz | 非空 |
| `updated_at` | timestamptz | 非空 |

约束：

```sql
CHECK (position BETWEEN 1 AND 5)
```

读取时最多返回 5 条；同一有效时段内不应出现重复 `position`。

## 6.9 `tags`

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `name` | varchar(50) | 唯一 |
| `slug` | varchar(60) | 唯一 |
| `status` | varchar(20) | `ACTIVE`、`DISABLED` |
| `created_at` | timestamptz | 非空 |

## 6.10 `content_tags`

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `content_id` | uuid | FK → contents |
| `tag_id` | uuid | FK → tags |
| `sort_order` | integer | 默认 0 |

主键：`(content_id, tag_id)`。

## 6.11 `content_recommendations`

人工推荐关系；无配置后台时由 SQL 维护。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `content_id` | uuid | 当前内容 |
| `recommended_content_id` | uuid | 被推荐内容 |
| `sort_order` | integer | 排序 |
| `created_at` | timestamptz | 非空 |

主键：`(content_id, recommended_content_id)`。

## 6.12 `comments`

评论和回复共用一张表。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `content_id` | uuid | FK → contents |
| `user_id` | uuid | FK → users |
| `parent_id` | uuid | 直接回复的评论，可空 |
| `root_id` | uuid | 所属顶级评论；顶级评论可空 |
| `reply_to_user_id` | uuid | 被回复用户，可空 |
| `floor_no` | bigint | 顶级评论楼层；回复可空 |
| `body` | varchar(2000) | 非空 |
| `like_count` | integer | 默认 0 |
| `reply_count` | integer | 默认 0 |
| `status` | varchar(20) | `VISIBLE`、`PENDING`、`HIDDEN`、`DELETED` |
| `created_at` | timestamptz | 非空 |
| `updated_at` | timestamptz | 非空 |

关键约束和索引：

```sql
CREATE UNIQUE INDEX uk_comments_floor
ON comments(content_id, floor_no)
WHERE parent_id IS NULL AND status <> 'DELETED';

CREATE INDEX idx_comments_content
ON comments(content_id, created_at ASC, id ASC)
WHERE parent_id IS NULL AND status = 'VISIBLE';

CREATE INDEX idx_comments_root
ON comments(root_id, created_at ASC, id ASC)
WHERE status = 'VISIBLE';
```

楼层号应在事务中按内容维度分配，不能使用全局序列直接作为楼层。

## 6.13 `content_actions`

内容点赞和收藏。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `user_id` | uuid | FK → users |
| `content_id` | uuid | FK → contents |
| `action_type` | varchar(20) | `LIKE`、`FAVORITE` |
| `created_at` | timestamptz | 非空 |

主键：`(user_id, content_id, action_type)`。

## 6.14 `comment_likes`

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `user_id` | uuid | FK → users |
| `comment_id` | uuid | FK → comments |
| `created_at` | timestamptz | 非空 |

主键：`(user_id, comment_id)`。

## 6.15 `author_follows`

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `user_id` | uuid | 关注人 |
| `author_id` | uuid | 被关注作者 |
| `created_at` | timestamptz | 非空 |

主键：`(user_id, author_id)`。

## 6.16 `submissions`

投稿草稿和审核状态。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `user_id` | uuid | 投稿人 |
| `channel_id` | uuid | 投稿栏目 |
| `title` | varchar(200) | 主标题 |
| `subtitle` | varchar(300) | 副标题，可空 |
| `cover_asset_id` | uuid | 封面，可空 |
| `body_blocks` | jsonb | 正文结构 |
| `body_text` | text | 纯文本 |
| `is_original` | boolean | 原创声明 |
| `status` | varchar(20) | `DRAFT`、`SUBMITTED`、`APPROVED`、`REJECTED`、`PUBLISHED` |
| `reject_reason` | varchar(500) | 可空 |
| `submitted_at` | timestamptz | 可空 |
| `reviewed_at` | timestamptz | 可空 |
| `published_content_id` | uuid | FK → contents，可空 |
| `version` | integer | 乐观锁版本 |
| `created_at` | timestamptz | 非空 |
| `updated_at` | timestamptz | 非空 |

保存草稿时携带 `version`，服务端使用乐观锁防止多端覆盖。

## 6.17 `submission_tags`

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `submission_id` | uuid | FK → submissions |
| `tag_id` | uuid | FK → tags |

主键：`(submission_id, tag_id)`。

## 6.18 `ai_tasks`

记录 AI 成文和改写任务。

| 字段 | 类型 | 约束与说明 |
| --- | --- | --- |
| `id` | uuid | PK |
| `user_id` | uuid | 发起用户 |
| `submission_id` | uuid | 关联草稿，可空 |
| `task_type` | varchar(30) | `PHOTO_TO_ARTICLE`、`REWRITE` |
| `status` | varchar(20) | `PENDING`、`RUNNING`、`SUCCEEDED`、`FAILED` |
| `request_payload` | jsonb | 输入快照 |
| `result_payload` | jsonb | 输出结果，可空 |
| `error_code` | varchar(50) | 可空 |
| `error_message` | varchar(500) | 可空 |
| `model_name` | varchar(100) | 使用模型 |
| `created_at` | timestamptz | 非空 |
| `started_at` | timestamptz | 可空 |
| `finished_at` | timestamptz | 可空 |

AI 输入输出需保留审计记录，但应设置保存期限并清理敏感图片。

## 7. 运营直接写库流程

推荐提供一个内部 CLI，例如：

```bash
zhijian-content import article.json
zhijian-content publish <content-id>
zhijian-content carousel set --position 1 --content-id <content-id>
```

CLI 本质仍是数据库事务，不需要建设 Web 后台，但比手写 SQL 更不容易漏字段。

### 7.1 发布事务

```sql
BEGIN;

INSERT INTO media_assets (...);

INSERT INTO contents (
  id,
  channel_id,
  author_id,
  title,
  subtitle,
  summary,
  cover_asset_id,
  display_variant,
  body_blocks,
  body_text,
  status,
  published_at,
  created_at,
  updated_at
) VALUES (
  :id,
  :channel_id,
  :author_id,
  :title,
  :subtitle,
  :summary,
  :cover_asset_id,
  :display_variant,
  :body_blocks::jsonb,
  :body_text,
  'PUBLISHED',
  now(),
  now(),
  now()
);

INSERT INTO content_stats (content_id)
VALUES (:id);

INSERT INTO content_tags (content_id, tag_id, sort_order)
SELECT :id, tag_id, sort_order
FROM jsonb_to_recordset(:tags::jsonb)
AS x(tag_id uuid, sort_order integer);

COMMIT;
```

### 7.2 配置首页轮播

```sql
BEGIN;

DELETE FROM home_carousel
WHERE position = :position
  AND is_enabled = true
  AND (ends_at IS NULL OR ends_at > now());

INSERT INTO home_carousel (
  id,
  content_id,
  image_asset_id,
  position,
  starts_at,
  ends_at,
  is_enabled,
  created_at,
  updated_at
) VALUES (
  gen_random_uuid(),
  :content_id,
  :image_asset_id,
  :position,
  now(),
  :ends_at,
  true,
  now(),
  now()
);

COMMIT;
```

建议写库后执行自动校验：

- `PUBLISHED` 内容必须有作者、频道、标题和正文。
- `SIDE_IMAGE`、`HERO_IMAGE` 必须有关联图片。
- 轮播图片必须存在且状态为 `READY`。
- 轮播位置必须为 1–5。
- 有效轮播总数不得超过 5。
- 推荐内容不能指向自己。
- 发布内容使用的标签必须处于 `ACTIVE`。

## 8. 首页查询建议

一期可由 `GET /home` 聚合三次数据库查询：

1. 查询可见频道。
2. 查询最多 5 条有效轮播。
3. 查询当前频道首屏信息流。

不要把轮播与信息流拼成一条 SQL；独立查询更容易控制索引和缓存。

轮播查询：

```sql
SELECT
  hc.id,
  hc.content_id,
  COALESCE(hc.title_override, c.title) AS title,
  hc.position,
  ma.id AS image_id,
  ma.url AS image_url,
  ma.width,
  ma.height,
  ma.alt_text
FROM home_carousel hc
JOIN contents c
  ON c.id = hc.content_id
 AND c.status = 'PUBLISHED'
JOIN media_assets ma
  ON ma.id = COALESCE(hc.image_asset_id, c.cover_asset_id)
 AND ma.status = 'READY'
WHERE hc.is_enabled = true
  AND hc.starts_at <= now()
  AND (hc.ends_at IS NULL OR hc.ends_at > now())
ORDER BY hc.position ASC
LIMIT 5;
```

## 9. 缓存与一致性

一期建议：

- `/home`：CDN/API 缓存 30–60 秒。
- 内容详情：缓存 5 分钟；互动计数可短暂延迟。
- 评论列表：不做公共 CDN 长缓存。
- 用户的 `viewer` 状态不能进入公共缓存，应由登录态单独查询或在网关层区分。
- 运营写库后可等待缓存自然过期；如需立即生效，再增加内部缓存刷新命令。

计数处理：

- 点赞和收藏必须依赖唯一约束保证幂等。
- 点赞事务同时写 `content_actions` 并更新 `content_stats`。
- 阅读事件按“用户/匿名设备 + 内容 + 时间窗口”去重。
- `content_stats` 只是展示计数，互动明细表才是事实来源。

## 10. 一期实施顺序

1. 建立 `channels`、`media_assets`、`authors`、`contents`、`content_stats`、`home_carousel`。
2. 完成首页与详情只读接口，替换前端 `src/data/content.ts`。
3. 建立用户登录、评论、互动相关表和接口。
4. 建立投稿、上传、AI 任务接口。
5. 编写受控内容导入 CLI 和 SQL 校验。
6. 最后接入缓存、审核策略和监控。

一期最小闭环必须包含：

- 内容写库后可在首页和详情展示。
- 最多 5 条热点轮播可通过写表调整。
- 用户可以登录、评论、点赞和收藏。
- 投稿可以保存草稿、调用 AI、提交审核。
- 投稿不能未经审核直接成为正式内容。

